---
type: project
status: active
goal: <цель работодателя своими словами — одна фраза>
opened: <YYYY-MM-DD>
aliases: ["<читаемое имя проекта>"]
---

# <Имя проекта>

> Группировка работы, названная **твоими** словами. Совпадать с деревом трекера не обязана — она нужна, чтобы отвечать на вопрос «чем я вообще занят», а не чтобы дублировать структуру компании.

## Цель

<На какую цель работодателя это работает — своими словами. Формулировка, которую не объяснишь коллеге за минуту, ничего не трассирует.>

## Что входит

<Границы: какие системы, сервисы, области. Что НЕ входит — тоже полезно назвать.>

## Состояние

<Где проект сейчас: одна-две фразы. Правится по ходу, не накапливается историей.>

## Задачи

```dataview
TABLE WITHOUT ID link(file.link, ticket) AS "Тикет", title AS "Задача", status AS "Статус"
FROM "10-governance/tasks"
WHERE type = "task" AND contains(string(project), this.file.name)
SORT status ASC
```

## Решения

<ADR по этому проекту — 10-governance/projects/<этот>/adr/ (ADR о развиваемой системе не кладём в её репозиторий: они раскрывают топологию и расположение секретов).>
